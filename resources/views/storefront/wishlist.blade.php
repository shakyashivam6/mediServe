<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Your wishlist · MediServe</title><style>
*{box-sizing:border-box}body{margin:0;color:#18332c;font:15px/1.5 system-ui,sans-serif;background:#f7faf8}.wrap{width:min(1060px,calc(100% - 30px));margin:auto}.head{height:72px;background:#fff;border-bottom:1px solid #e6eeeb}.head .wrap{height:100%;display:flex;align-items:center;justify-content:space-between}.brand{font-size:22px;font-weight:850;color:#087f5b;text-decoration:none}.brand span{color:#18332c}a{color:#087f5b;text-decoration:none}.title{margin:35px 0 20px}.title h1{margin:0;font-size:29px}.title p{color:#70817c}.grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:15px}.card{background:#fff;border:1px solid #e6eeeb;border-radius:14px;overflow:hidden}.pic{height:160px;background:#f3f8f5;display:flex;align-items:center;justify-content:center}.pic img{width:100%;height:100%;object-fit:contain;padding:14px}.pic span{font-size:45px}.body{padding:14px}.maker,.muted{font-size:12px;color:#70817c}.name{font-weight:750;min-height:42px;margin:5px 0}.price{font-size:18px;font-weight:800;margin:8px 0}.buttons{display:flex;gap:8px}.buttons form{flex:1}.buttons button{width:100%;padding:9px;border-radius:8px;border:0;font-weight:700;cursor:pointer}.add{background:#087f5b;color:#fff}.remove{background:#fcecef;color:#a6354a}.empty{background:#fff;border:1px dashed #cbdad4;border-radius:14px;text-align:center;padding:50px;color:#70817c}@media(max-width:780px){.grid{grid-template-columns:repeat(3,1fr)}}@media(max-width:560px){.grid{grid-template-columns:repeat(2,1fr);gap:10px}.pic{height:130px}}
</style></head><body><header class="head"><div class="wrap"><a class="brand" href="{{ route('home') }}">medi<span>Serve</span></a><a href="{{ route('cart.index') }}">🛒 View cart</a></div></header><main class="wrap"><div class="title"><h1>♡ Your wishlist</h1><p>Your saved health essentials, all in one place.</p></div>
@if($items->isEmpty())<div class="empty"><div style="font-size:45px">♡</div><h2>No saved products yet</h2><p>Tap the heart on a product to keep it here.</p><a href="{{ route('home') }}">Explore products →</a></div>@else<div class="grid">@foreach($items as $product)<article class="card"><div class="pic">@if(json_decode($product->images ?: '[]', true)[0] ?? null)<img src="{{ json_decode($product->images, true)[0] }}" alt="{{ $product->name }}">@else<span>🧴</span>@endif</div><div class="body"><div class="maker">{{ $product->manufacturer ?: 'MediServe' }}</div><div class="name">{{ $product->name }}</div><div class="price">{{ $product->price !== null ? '₹'.number_format((float)$product->price, 2) : 'Price on request' }}</div><div class="buttons"><form method="POST" action="{{ route('cart.add', $product->id) }}">@csrf<button class="add">Add to cart</button></form><form method="POST" action="{{ route('wishlist.toggle', $product->id) }}">@csrf<button class="remove">♥</button></form></div></div></article>@endforeach</div>@endif
</main></body></html>
